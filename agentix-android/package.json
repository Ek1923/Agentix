{
  "name": "agentix-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android shell around the Agentix web build",
  "type": "module",
  "scripts": {
    "build:web": "npm --prefix ../agentix-web run build",
    "sync": "npm run build:web && cap sync android",
    "open": "cap open android",
    "run": "npm run sync && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/haptics": "^8.0.2"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
