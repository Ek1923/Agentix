import type { Manifest } from '../../core/plugin-host/types'

export const manifest: Manifest = {
  id: 'backtest',
  name: 'Backtest',
  icon: 'BarChart3',
  version: '1.0.0',
  requiresAI: false,
}
